function mango(quantity, price){
  var sale = 0;
  var prices = 0;
  for (i=1; i-1 < quantity; i++) {
    if (i % 3 == 0) {
      sale++;
      };
    console.log(i); 
      console.log(sale);
    };
   return (quantity*price) - (price*sale);
  }
