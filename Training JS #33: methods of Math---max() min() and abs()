function maxMin(arr1,arr2){
  //coding here...
  var ops = [];
    for (var i=0;i<arr1.length;i++) {
   ops.push(Math.abs(arr1[i]-arr2[i]));
        
      }
  var maxMinArr = [];
   maxMinArr.push(Math.max(...ops));
    maxMinArr.push(Math.min(...ops));
  return maxMinArr;
}
